name: CI Pipeline

on:
  push:
    branches: [ main, develop, '*' ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install flake8 isort black mypy pytest

      - name: Run Black (code formatting - disabled for now)
        run: echo "Black check skipped temporarily"


      - name: Run Isort (sort imports)
        run: isort --check-only --diff .


      - name: Run Flake8 (style)
        run: flake8 .

      - name: Run MyPy (type checking)
        run: mypy .

      - name: Run Tests
        run: pytest --cov=./
